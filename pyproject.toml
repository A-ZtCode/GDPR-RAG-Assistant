[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gdpr-rag-assistant"
version = "0.1.0"
description = "RAG assistant for GDPR regulatory Q&A"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]